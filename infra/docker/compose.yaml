version: "3.9"
services:
  api:
    image: bgent-api:dev
    build:
      context: ../../apps/client/api
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=dev
  web:
    image: bgent-web:dev
    build:
      context: ../../apps/client/web
    ports:
      - "3000:3000"
    environment:
      - APP_ENV=dev
  # db ???„ìš” ??ì¶”ê?
networks:
  default:
    name: bgent-net